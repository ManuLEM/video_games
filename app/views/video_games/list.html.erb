<%= form_tag(add_game_path, method: :post) do -%>
  <%= text_field_tag 'title' %>
  <%= submit_tag 'Add Game' %>
<% end -%>
<ul>
  <% @video_games.each do |game| %>
    <li>
      <%= link_to game.title, video_game_path(game) %>
    </li>
  <% end %>
</ul>